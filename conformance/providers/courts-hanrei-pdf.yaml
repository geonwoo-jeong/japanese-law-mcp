schemaVersion: 1
rows:
  - providerId: courts-hanrei-pdf
    capabilityId: judicial-decision.case-citation.extract
    majorVersion: 1
    operation: "GET /assets/hanrei/{document}.pdf"
    interfaceSotIds:
      - SOT-IF-077
      - SOT-IF-068
      - SOT-IF-070
      - SOT-IF-071
      - SOT-IF-074
    budgetSotId: SOT-IF-070
    budgetKey: judicial-citation-pdf
    concurrencyGroup: courts-hanrei-pdf
    artifactType: PDF
    fixtureSet: judicial-decision-case-citation-extract-v1
    requiredCases:
      - descriptor
      - capability-binding
      - outbound-request
      - provenance
      - resource-ref-roundtrip
      - empty-vs-not-found
      - unsupported-query
      - error-normalization
      - secret-non-exposure
      - response-bytes-limit
      - decompressed-bytes-limit
      - entries-or-objects-limit
      - depth-limit
      - parse-timeout
      - concurrency-limit
      - cancellation
      - contract-changed
    notApplicableCases:
      - case: authentication
        reason: このプロバイダーは認証情報を使用しないため。
      - case: page-invariants
        reason: 一つの PDF から抽出結果を返し、共通ページ情報を使用しないため。
      - case: continuation-roundtrip
        reason: 継続取得を使用しないため。
      - case: continuation-tamper
        reason: 継続取得を使用しないため。
      - case: continuation-expired
        reason: 継続取得を使用しないため。
    supportsContinuation: false
    supportsAuth: false
    publicErrorSet:
      - invalid_argument
      - not_found
      - unsupported_capability
      - configuration_required
      - rate_limited
      - source_timeout
      - source_unavailable
      - source_busy
      - source_contract_changed
      - invalid_source_response
      - source_response_too_large
      - source_processing_limit
      - unsafe_source_content
    parserContractVersion: "1.0.0"
    implementedBy: github.com/geonwoo-jeong/japanese-law-mcp/internal/source/courts/hanreipdf
    conformanceTarget: ./internal/source/courts/hanreipdf
    status: implemented
