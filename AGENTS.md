# Japanese Law MCP エージェント作業規則

このファイルは、リポジトリ全体で作業する人と AI に適用する。

## 1. 判断基準の優先順位

作業を始める前に、次の順序で定義元を確認する。

1. [`docs/development-principles.md`](docs/development-principles.md)
2. [`sot/00-index.md`](sot/00-index.md) と、作業対象に関係する各領域の SOT
3. 実装、テスト、生成物およびその他の文書

上位の定義と下位の成果物が一致しない場合は、上位の定義を基準とする。

## 2. 開発原則は変更しない

`docs/development-principles.md` は読み取り専用の基準である。内容の基準値は `docs/development-principles.sha256` で固定する。

通常の開発、リファクタリング、SOT の整備、仕様変更、バグ修正または自動更新の一部として、開発原則またはそのチェックサムを編集、移動、改名、整形または生成し直してはならない。

依頼内容、SOT または実装が開発原則と両立しない場合は、開発原則を変更せず、矛盾する箇所と作業できない理由を報告する。

## 3. SOT を確認してから作業する

実装または文書を変更する前に、対象となる事実の定義元である SOT ID を特定する。

- 定義元がある場合は、その規定、制約、関連 SOT および検証方法を確認する。
- 定義元がない場合は、実装へ事実を埋め込む前に、または同じ変更の中で SOT を追加する。
- `草案` はレビュー対象であり、実装とリリースの基準にしない。
- `有効` な SOT だけを、実装と検証の基準として扱う。
- 採用済みで未実装の理想状態は SOT に残し、実装との差分は Wiki で追跡する。
- 未確定の案、比較中の選択肢および判断理由は Wiki または ADR で扱う。

コード、テスト、スキーマまたは設定ファイルを、SOT と競合する別の定義元にしてはならない。

## 4. SOT を更新する条件

採用する理想状態、公開する振る舞い、モデル、境界、外部 API との対応、開発規則または提供方法が変わる場合は、関係する SOT を更新する。

SOT の更新では、次を守る。

- 一つの Markdown に、単独で採用、変更または廃止できる規定を一つだけ置く。
- 新しい規定には、その領域で未使用の次の二桁番号と新しい SOT ID を割り当てる。
- 規定の意味または適用範囲を置き換える場合は、既存 ID を流用しない。
- `00-index.md` には規則ファイルへの相対リンクだけを追加し、規則名、SOT ID、状態または本文を複製しない。
- 同じ事実を複数の SOT に記載せず、別の場所では定義元を参照する。
- 外部仕様の事実を変更する前に、公式の一次資料で現在の内容を確認する。
- SOT、実装および検証物の作成順は問わないが、変更完了時に提供対象の範囲を一致させる。

## 5. 変更後の照合

変更を完了する前に、次を確認する。

1. 変更した成果物から適用した SOT ID と検証方法へ到達できる。
2. SOT の ID、状態および `## 規定` が各規則ファイルに一つずつある。
3. 領域内のファイル番号とリポジトリ全体の SOT ID が重複していない。
4. 各領域の索引が、同じ領域の番号付き規則ファイルを一度ずつ参照する。
5. Markdown の相対リンクが存在するファイルを指す。
6. 同じ事実の定義が SOT、コード、テストまたは索引へ重複していない。
7. `SOT-ENG-020` に定義した、変更対象に適用できる検証ゲートが成功する。
8. macOS または Linux では、リポジトリのルートで `shasum -a 256 -c docs/development-principles.sha256` を実行する。Windows では、`Get-FileHash` の結果を `docs/development-principles.sha256` の先頭列と比較する。

検証に失敗した場合は、開発原則を除く定義元または派生成果物を修正し、もう一度照合する。

## 6. コーディングスタイルと静的解析

Go コードのスタイル、リンター、静的解析および抑制方法は、[`SOT-ENG-019`](sot/50-engineering/19-static-analysis-and-coding-style.md) に従う。

人または AI が生成したコードを区別せず、[`SOT-ENG-020`](sot/50-engineering/20-verification-gate.md) の適用可能な検証を完了する。

## 7. 日本語

自然言語の記述は、[`開発原則 7`](docs/development-principles.md#7-日本語による記述)に従う。

ドキュメント、コードコメント、利用者向け文字列、エラーメッセージ、コミットメッセージの説明文、リリース情報およびレビュー記録は日本語で記述する。

プログラミング言語の構文と識別子、外部仕様の名称と値、コマンド、ファイルパス、および Conventional Commits の種別など機械処理に必要なメタデータは、日本語原則の適用対象外とする。これらは日本語の文中でも定義どおりの表記を使用する。

## 8. Git フックと CI

clone 後に `.githooks/manage install` を実行し、`.githooks/manage check` で当該リポジトリの `pre-commit` と `pre-push` が有効であることを確認する。ambient の Go build cache に依存する `go run ./cmd/git-hook` を管理操作の直接の入口にしない。Go module cache の扱い、利用者端末の資源上限を含む段階ごとの検査対象、および CI の責任は [`SOT-ENG-027`](sot/50-engineering/27-resource-aware-verification-stages.md) に従う。

Git の `--no-verify` でローカルフックを迂回しても、検証が免除されたとは扱わない。CI は clean checkout に対して [`SOT-ENG-020`](sot/50-engineering/20-verification-gate.md) の全ゲートを再実行し、その成功を変更完了の条件とする。

## 9. コミット

コミット前に、開発原則のチェックサム、対象 SOT と成果物の一致、および適用可能な検証の成功を確認する。

コミットメッセージは Conventional Commits の形式を使い、種別以外の件名と本文を日本語で記述する。
