# SOT-IF-005 SOT-IF-021 SOT-ENG-015: stdio では listenAddress の変更を起動前に拒否する。
env XDG_CONFIG_HOME=$WORK/config-home
! exec japanese-law-mcp-test --listen-address=127.0.0.1:9090
stderr 'listenAddress'
! stdout .
